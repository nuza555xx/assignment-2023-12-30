export enum EntityVisibility {
  HIDDEN = 'hidden',
  PUBLISH = 'publish',
  PENDING = 'pending',
  DELETE = 'deleted',
}

export enum Sorting {
  ASC = 'asc',
  DESC = 'desc',
}

export enum ActionBy {
  VIEW = 'view',
  COMMENT = 'comment',
}
